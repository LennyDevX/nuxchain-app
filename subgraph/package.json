{
  "name": "nuxchain-subgraph",
  "version": "0.43",
  "description": "Nuxchain Subgraph - The Graph indexer for Nuxchain smart contracts on Polygon",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy nuxchain --node https://api.studio.thegraph.com/deploy/ --deploy-key c2a9797ca09c319ef341948f6dd206b1 --version-label v0.43",
    "create-local": "graph create --node http://localhost:8020/ nuxchain",
    "remove-local": "graph remove --node http://localhost:8020/ nuxchain",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nuxchain"
  },
  "dependencies": {},
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.0",
    "@graphprotocol/graph-ts": "^0.38.1"
  }
}
